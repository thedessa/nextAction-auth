package dev.ifrs;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeNextActionServiceIT extends NextActionServiceTest {

    // Execute the same tests but in native mode.
}